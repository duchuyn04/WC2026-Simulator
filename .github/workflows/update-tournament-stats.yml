name: Update Tournament Stats

on:
  schedule:
    - cron: "15 * * * *"
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: update-tournament-stats
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Fetch FIFA tournament statistics
        run: npm run fetch:tournament-stats

      - name: Commit updated statistics
        id: commit
        shell: bash
        run: |
          if git diff --quiet -- data/fifa-tournament-stats.json; then
            echo "Tournament statistics are already up to date."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/fifa-tournament-stats.json
          git commit -m "chore: update FIFA tournament stats"
          git push
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Deploy updated statistics
        if: ${{ steps.commit.outputs.changed == 'true' }}
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run deploy.yml --ref main
